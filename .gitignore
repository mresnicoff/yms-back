node_modules
# Keep environment variables out of version control
.env

/generated/prisma
coverage/
logs/
*.log

# Local database dumps — may contain real data, never commit these.
# (Prisma migration files under prisma/migrations/**/*.sql are unaffected.)
/backup*.sql
